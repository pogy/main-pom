<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.opentae.data.daifa.interfaces.DaifaGgoodsTasksMapper" >
    <resultMap id="BaseResultMap" type="com.opentae.data.daifa.beans.DaifaGgoodsTasks" >
	        <id column="permission_id" property="permissionId" jdbcType="DECIMAL" />
                <result column="namespace" property="namespace" jdbcType="VARCHAR" />
                <result column="permission_tag" property="permissionTag" jdbcType="VARCHAR" />
                <result column="create_time" property="createTime" jdbcType="TIMESTAMP" />
                <result column="create_user_id" property="createUserId" jdbcType="DECIMAL" />
                <result column="status" property="status" jdbcType="DECIMAL" />
                <result column="permission_comment" property="permissionComment" jdbcType="VARCHAR" />
                <result column="parent_pid" property="parentPid" jdbcType="DECIMAL" />
        </resultMap>

    <sql id="Example_Where_Clause" >
<!--
      WARNING - @mbggenerated
      This element is automatically generated by MyBatis Generator, do not modify.
    -->
    <where >
      <foreach collection="oredCriteria" item="criteria" separator="or" >
        <if test="criteria.valid" >
          <trim prefix="(" suffix=")" prefixOverrides="and" >
            <foreach collection="criteria.criteria" item="criterion" >
              <choose >
                <when test="criterion.noValue" >
                  and ${criterion.condition}
                </when>
                <when test="criterion.singleValue" >
                  and ${criterion.condition} #{criterion.value}
                </when>
                <when test="criterion.betweenValue" >
                  and ${criterion.condition} #{criterion.value} and #{criterion.secondValue}
                </when>
                <when test="criterion.listValue" >
                  and ${criterion.condition}
                  <foreach collection="criterion.value" item="listItem" open="(" close=")" separator="," >
                    #{listItem}
                  </foreach>
                </when>
              </choose>
            </foreach>
          </trim>
        </if>
      </foreach>
    </where>
  </sql>


    <sql id="Update_By_Example_Where_Clause" >
<!--
      WARNING - @mbggenerated
      This element is automatically generated by MyBatis Generator, do not modify.
    -->
    <where >
      <foreach collection="example.oredCriteria" item="criteria" separator="or" >
        <if test="criteria.valid" >
          <trim prefix="(" suffix=")" prefixOverrides="and" >
            <foreach collection="criteria.criteria" item="criterion" >
              <choose >
                <when test="criterion.noValue" >
                  and ${criterion.condition}
                </when>
                <when test="criterion.singleValue" >
                  and ${criterion.condition} #{criterion.value}
                </when>
                <when test="criterion.betweenValue" >
                  and ${criterion.condition} #{criterion.value} and #{criterion.secondValue}
                </when>
                <when test="criterion.listValue" >
                  and ${criterion.condition}
                  <foreach collection="criterion.value" item="listItem" open="(" close=")" separator="," >
                    #{listItem}
                  </foreach>
                </when>
              </choose>
            </foreach>
          </trim>
        </if>
      </foreach>
    </where>
  </sql>

    <select id="selDaifaGoodsGroupByShop" resultType="com.opentae.data.daifa.beans.GgoodsByStore">

        select
            market_id marketId,
            market_name marketName,
            floor_id floorId,
            floor_name floorName,
            store_id storeId,
            store_num storeNum,
            sum(goods_num) goodsNum
        from daifa_ggoods_tasks
        where seller_id = ${sellerId}
          and use_status = 1
          and end_status = 0
          and allocat_status = 0
          and goods_code is not null
          and (youhuo_date is null
            or (if(youhuo_date is not null,date_format(youhuo_date,"%Y%m%d")-create_date &lt;=0 or date_format(now(),"%Y%m%d")-create_date &gt;0,true)))
        GROUP BY store_id

    </select>

</mapper>
