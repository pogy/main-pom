<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.opentae.data.mall.interfaces.ActivityDrawPrizePoolMapper" >

    <update id="addProvide">
        UPDATE activity_draw_prize_pool SET drawed_num = drawed_num + 1 WHERE pem_id = #{pemId} AND rank = ${rank}
    </update>

</mapper>