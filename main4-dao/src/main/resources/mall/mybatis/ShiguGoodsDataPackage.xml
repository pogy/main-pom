<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.opentae.data.mall.interfaces.ShiguGoodsDataPackageMapper">

    <!-- 查询数据包list -->
    <select id="selGoodsPackageList" resultType="com.opentae.data.mall.beans.ShiguGoodsDataPackage">
      SELECT
        p.*
    FROM
        shigu_goods_data_package p
    WHERE
     p.user_id = #{userId}
    ORDER BY p.remark1 DESC
    limit #{startIndex},#{pageSize}
    </select>

    <!-- 查询数据包count -->
    <select id="selGoodsPackageCount" resultType="int">
        SELECT
          count(p.data_package_id)
        FROM
          shigu_goods_data_package p
        WHERE
          p.user_id = #{userId}
    </select>

    <!-- 查询数据包 -->
    <select id="selGoodsPackageListByIds" resultType="com.opentae.data.mall.beans.ShiguGoodsDataPackage">
      SELECT
        p.*
      FROM
        shigu_goods_data_package p
      WHERE p.data_package_id in
        <foreach collection="packageIds" index="index" item="item" open="("
                 separator="," close=")">
            #{item}
        </foreach>
    </select>

</mapper>